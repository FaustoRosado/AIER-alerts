# Zero-Trust Hybrid AI Orchestrator - Cloud Dependencies (Lightweight)
# No spacy/presidio - PHI detection happens locally before routing to cloud

# Core web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# HTTP client for backend communication
httpx>=0.26.0

# For streaming responses
sse-starlette>=1.8.0

# Configuration management
python-dotenv>=1.0.0

# Monitoring and metrics
prometheus-client>=0.19.0

# Type hints
typing-extensions>=4.9.0
